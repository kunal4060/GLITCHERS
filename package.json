{
  "name": "nexa-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "NEXA: AI Student Life Companion (powered by NIA) - Monorepo",
  "workspaces": [
    "shared",
    "backend",
    "mobile"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "backend:dev": "npm run dev --workspace=backend",
    "mobile:start": "npm run start --workspace=mobile"
  },
  "devDependencies": {
    "jpeg-js": "^0.4.4",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "react-native": "^0.76.9"
  }
}
